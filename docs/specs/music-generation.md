# 音楽生成の仕組み(2026-08-06 決定)

会話で詰めた設計決定。実装プランは [docs/plans/music-generation-engine.md](../plans/music-generation-engine.md)。
**現在の実装の流れの図解は [music-generation-flow.md](music-generation-flow.md)**(この文書は決定の記録、あちらは挙動の説明)。

## 全体像

LLM(Claude API)がプリセット(要素プール)をもとに、毎回スタイルプロンプト+英語歌詞+日本語訳を生成し、Suno に `customMode: true` で渡す。生成に使ったプリセットをタスク単位で記録し、ユーザーの評価(👍/👎)はプリセット別の集計として次回生成のプロンプトに注入され、毎日良くなっていく(2026-08-08 変更: 当初の「好みプロファイル」文書方式を廃止し、評価の反映を使用プリセット×評価集計に一本化)。

```
評価(👍/👎) ────→ プリセット別 👍/👎 集計(task_presets × rating を毎回算出)
                        │
プリセット(DB) ──→ LLM(Sonnet 5)──→ スタイル + 英語歌詞 + 日本語訳 + タイトル + 使用プリセット
冒険日判定(20%) ─→                          │
                                customMode: true で Suno へ
                                            │
                              2 曲完成 → 1 曲目のみ data/ へ保存
```

## 決定事項

| 項目 | 決定 |
|---|---|
| プロンプトを組み立てる頭脳 | LLM(Claude API、`claude-sonnet-5`) |
| 歌詞 | LLM が英語で作詞し `customMode: true` で Suno に渡す。日本語訳も同時に生成し、両方保存 |
| 評価 | 曲ごとに 👍/👎 の 2 択。未評価は「普通」扱い(当初あった ★ お気に入りは 2026-08-07 に廃止、既存の ★ は 👍 に変換) |
| 評価の反映 | 使用プリセットをタスク単位で記録(`task_presets`)し、プリセット別の 👍/👎 を生成のたびに全期間集計して要素プール行に併記(👍 優先・👎 回避を指示。保存値なしのステートレス方式で、評価変更が次の生成に即反映)。2026-08-08 変更 — 当初の「好みプロファイル」文書を LLM が育てる方式は廃止(自由文書は妥当性を検証しにくく、データ+集計の方が挙動を追跡・制御しやすい。`profile` テーブルと過去データは履歴として残す) |
| プリセット | ジャンル・楽器・ムード・テンポ・歌声(2026-08-07 追加)を DB で管理し管理画面から編集。毎日の自動生成では全プールから LLM が評価を踏まえて自由に選ぶ。手動リクエスト時はユーザーが選択。2026-08-09 変更: 楽器(instrument)カテゴリを廃止 — プールに提示すると LLM が各カテゴリから律儀に拾ってジャンルの編成と喧嘩しやすく(実例: acoustic folk + rock に synthesizer)、評価の帰属も 1 曲あたりの採用要素が多いほど薄まるため。楽器プリセットが無くても LLM は style 内でジャンルに合う具体的な楽器を自由に書く(スキーマの style 説明に楽器を含む指示は維持)。既存 DB の instrument 行は起動時の一回限りマイグレーション(`settings` の `migration_drop_instrument_presets` で実施済みを記録)で削除し、`task_presets` のスナップショット・評価履歴は残置。楽器単位の好み学習が将来必要になれば音源解析(Gemini)タスク等の実測ベースで再導入する |
| 歌声(2026-08-07 追加) | プリセットの `vocal` カテゴリで歌手の声の特徴(性別 × 声質 × 年齢感 + デュエット・聖歌隊等)を管理。歌詞ありの生成では style に歌声の特徴を必ず含め、直近と偏らないよう幅広い声色を試すよう LLM に指示する |
| ランダム要素 | 毎日「1 要素は普段と違うものを入れる」+ 確率 20%(設定変更可)で「冒険日」として大きく外す |
| 自動生成の量 | 1 日 1 リクエスト(12 クレジット)。Suno は 2 曲返すが同一歌詞・スタイルのバリエーションのため 1 曲目のみ採用(2026-08-07 変更。クレジット消費はリクエスト単位で不変)。2026-08-09 変更: 1 日 3 曲(設定 `daily_count`、1〜10)に増量 — スケジューラが 1 曲ずつ順次リクエストし、その日の生成済み数を `last_daily_count` に記録して途中失敗・再起動時は残数だけ追い生成する(手動トリガ `POST /api/daily/run` は従来どおり 1 回 1 曲で API 不変更) |
| 実行時刻 | 西海岸時間(America/Los_Angeles)朝 6:00。サーバー停止中に跨いだ場合は起動時に追い生成 |

## 生成フロー(毎日の自動生成)

1. スケジューラが PT 6:00 を検知(停止中に跨いだら起動時に追い生成)。2026-08-09 変更: 1 日の曲数 `daily_count`(既定 3)の残数分だけ以下 2〜6 を 1 曲ずつ順次実行する。順次のため 2 曲目以降のプロンプトには同日の前の曲のスタイル・リアルワードが既存の仕組みで注入され、曲間の重複を避けられる(直近スタイルの注入は 5 → 10 件に拡大)
2. 冒険日判定: 確率 20%(設定値)。2026-08-09 の複数曲化以降、判定は 1 曲ごと(「冒険日」の文言は維持しているが、実質は曲単位の冒険判定)
3. **外部コンテキスト取得(2026-08-07 追加)**: ニュース(Google News RSS 日本版)を取得し「今日のコンテキスト」としてプロンプトに注入。取得失敗やソース OFF(設定画面で切替)ならセクション無しで続行し、生成は止めない。注入は毎日の自動生成のみ(手動リクエストは対象外)。天気(Open-Meteo)も併せて注入していたが 2026-08-09 に削除(生成への寄与が薄いため。コンテキストはニュースに一本化)。2026-08-09 変更: ニュースのソースを Google News RSS 日本版から米国版(英語)へ切り替え — 歌詞は英語で生成するため、海外英語メディアの見出しの方がテーマ語(リアルワード)や雰囲気が英語歌詞に自然に馴染む。XML 構造は同一のためパーサ・件数上限(8 件)・失敗時スキップは不変更
4. **LLM コール(生成)**: 入力 = プリセット全プール(プリセット別の 👍/👎 集計を併記。2026-08-08 変更、旧: 好みプロファイル)+ 今日のコンテキスト + 直近数曲のスタイル(重複回避用)+ リアルワードの使用制限 + モード(通常/冒険)。出力(JSON) = スタイルプロンプト(英語)/タイトル/英語歌詞/日本語訳/使用プリセット(2026-08-08 追加 — 提示値をそのまま写す指示。サーバーで `presets` と照合して `task_presets` に保存し、不一致は警告ログのみ)/狙いの説明(日本語、管理画面表示用)/リアルワード(曲の中心となった語、英語小文字 5〜8 個)
5. Suno へ `customMode: true`(style / prompt=歌詞 / title / model)で送信
6. 完了検知〜音源保存は既存フローのまま。生成された 2 曲のうち 1 曲目のみ採用・保存する(2026-08-07 変更)。歌詞・訳・スタイル・狙いはタスク単位で保存

かつては生成の前に **LLM コール(プロファイル更新)** があったが 2026-08-08 に廃止(評価の反映をプリセット集計に一本化し、毎朝の LLM コールが 1 回減った)。

### リアルワードの使用制限(2026-08-07 追加)

似たテーマの曲の連続生成を防ぐ仕組み。全モード(daily / daily_adventure / manual)共通で `generateSongPlan` が一元処理する。

- 生成のたびに LLM が出力した `realWorldWords` を `real_world_words` テーブルへ保存(小文字正規化)
- 生成前に直近ウィンドウ(既定 30 日、`word_window_days`)の使用回数を集計し、上限(既定 2 回、`word_max_uses`)に達した語は「使用禁止ワード」、残り 1 回の語は「残り 1 回のワード」としてプロンプトに注入。ウィンドウを外れた語は自動的に再利用可能になる(物理削除なし)
- manual ではユーザーの選択・自由リクエストが禁止ワードより優先されると明記して注入する
- **検証リトライ**: 出力の realWorldWords に禁止ワードが残っていたら指示を強めて 1 回だけ再生成。それでも残る場合は警告ログのみで採用(生成を止めない)
- 使用状況は管理画面のパラメータ一覧ページ(リアルワード表)で確認できる

### 手動リクエスト

プリセットを選択(+自由テキスト)→ 同じ LLM 生成 → 同じ Suno フロー。手動生成の曲への評価も同じプリセット集計に反映される(使用プリセットはユーザー選択と LLM 出力の照合結果の和集合を記録)。ただし manual の生成プロンプトには評価集計を注入しない — ユーザー指定が最優先で、集計で誘導しない(2026-08-08)。

### アーティスト経由生成(2026-08-09 追加)

好きなアーティストの特定の曲を「リファレンス楽曲」として選び、それに似た新曲を作る 3 つ目の生成経路(`mode = 'artist'`)。アーティストを登録すると iTunes からその人の曲が取り込まれ、曲を選んで生成する。

| 項目 | 決定 |
|---|---|
| 楽曲データソース | **iTunes Search API**。API キー不要・無料で `.env` にも本番のシークレット管理にも影響が出ない(Spotify はキー必須、MusicBrainz は 1 req/s 制限+曲の網羅性が中途半端)。日本語で検索してもヒットするが返る名前はローマ字表記のことが多く(「米津玄師」→「Kenshi Yonezu」)別名義も混ざるため、**候補一覧から選ばせて取り違えを防ぐ**。`lookup` の `limit` は 200 で頭打ち。同名の重複(シングル版・アルバム版・ベスト盤)が 2 割前後あるため曲名の正規化(trim + 小文字化)で最古のリリースを残す。track 行の `artistId` が登録アーティストと一致しないコラボ曲も本人の曲なので除外しない |
| Suno への渡し方 | **固有名詞は Suno に渡さない**。Suno はプロンプト内のアーティスト名・曲名をモデレーションで弾く(`kieai.ts` の `FAILURE_STATUSES` にある `SENSITIVE_WORD_ERROR`)。そのためこの経路の肝は「参照曲を名指しで伝える」ことではなく、**LLM に参照曲の音楽的特徴(ジャンル・テンポ・リズム・コード進行の雰囲気・楽器編成・ボーカルの質感・プロダクション)を具体的な英語の音楽用語へ翻訳させ、固有名詞を落とした style を書かせる**こと。送信前にサーバー側で混入を検査する(下記) |
| 混入の検査 | style / title / lyrics にアーティスト名、title に曲名が含まれていないかを検査(小文字化。ASCII 名は語境界で見て「Air」が「airplane」に誤ヒットしないようにし、3 文字未満の名前は誤検出が多いので見ない)。曲名は普通の単語のこと(「Lemon」等)があるため歌詞は見ない。違反時は禁止ワードと同じく指示を強めて**1 回だけ再生成**し、それでも残れば警告ログのみで続行(Suno 側で弾かれれば FAILED として観測できる) |
| 歌詞 | 原曲の複製・翻訳・言い換えを明示的に禁止し、テーマ・情景の参考に留めた完全な新作にする(著作権上の配慮) |
| LLM が曲を知らない場合 | そのアーティストの一般的な作風から推定してよいと指示し、**何を根拠にしたか(曲を知っている / 作風からの推定)を `intent` に書かせる**。結果の妥当性をユーザーが判断できるようにするため |
| 既存の学習ループとの関係 | 要素プールを LLM に提示しない(`usedPresets` は空)ので、この経路の 👍/👎 はプリセット集計を汚さない。直近スタイルの重複回避は「参照曲に似せる」目的と矛盾するので注入しない。リアルワード制限は注入するが、参照曲の再現と衝突する場合はリファレンスを優先すると明記する。歌声も「幅広い声色を試す」ではなく「参照曲のボーカルの質感に寄せる」に差し替える |
| 非目標(今回はやらない) | 毎日の自動生成への織り込み / アーティスト別・曲別の評価集計と学習への反映(`tasks.artist_id` は記録だけ先に用意)/ Web 検索・音源解析による参照曲の特徴補強 |

## データモデル(追加・変更)

- `tasks` に追加: `mode`('daily' | 'daily_adventure' | 'manual' | 'artist')、`style`、`style_ja`(スタイルの日本語訳。2026-08-07 追加)、`lyrics`(英語)、`lyrics_ja`、`title`、`intent`(LLM の狙い説明)、`llm_model`・`llm_prompt`(生成に使った LLM モデル名と入力全文。2026-08-07 追加、管理画面の楽曲詳細に生成パラメータとして表示)
- `tracks` に追加: `rating`(NULL / 1 / -1)
- `presets`(新規): `category`(genre / mood / tempo / vocal。vocal は 2026-08-07 追加、instrument は 2026-08-09 廃止 — 既存行は起動時の一回限りマイグレーションで削除)、`value`(英語・プロンプト用)、`label_ja`(表示用)。初期セットはこちらで用意し、起動時に DB に 1 件も無いカテゴリだけ投入する(既存カテゴリ内の編集・削除は再投入で復活しない)
- `profile`(新規 → 2026-08-08 廃止): プロファイル文書を版として積む方式だったが、更新・注入・閲覧の機能を削除。テーブルと過去データは履歴として残す
- `task_presets`(新規、2026-08-08): `task_id`、`preset_id`(集計用。参照強制なし)+使用時点のスナップショット `category` / `value` / `label_ja`(プリセットが編集・削除されても表示できる)。生成に使ったプリセットをタスク単位で記録し、`tracks.rating` との join で評価集計の元になる
- `settings`(新規): key-value(`adventure_probability`、`daily_enabled`、実行時刻・タイムゾーン等)。将来 iPhone から変更できるように DB 持ち。2026-08-07 追加: `context_news`(ニュース ON/OFF)、`word_max_uses` / `word_window_days`(リアルワード制限)。管理画面の設定ページ(/admin/settings.html)から変更でき、次の生成から即反映。`context_weather` / `weather_city` / `weather_lat` / `weather_lon`(天気の ON/OFF と都市)は天気注入の削除(2026-08-09)に伴い廃止 — 既存行はコードから参照されず残るだけ
- `real_world_words`(新規、2026-08-07): `task_id`、`word`、`created_at`。曲の中心となった語をタスク単位で記録し、使用回数制限の集計に使う
- `artists` / `artist_songs`(新規、2026-08-09): アーティスト経由生成の登録アーティスト(`name` は iTunes の正式表記で UNIQUE、`itunes_artist_id`、`genre`)と、その楽曲(`artist_id`、`title`、`album`、`release_year`、`genre`、`itunes_track_id`。`UNIQUE (artist_id, title)` で再取得時は新譜だけ増える)
- `tasks` に追加(2026-08-09): `artist_id` / `artist_song_id`(将来のアーティスト別集計用。参照強制なし)+ `ref_artist_name` / `ref_song_title`(表示用スナップショット。`task_presets` と同じ考え方で、アーティストを削除しても過去のタスク・楽曲詳細に出せる)

## API(追加)

- `POST /api/tracks/:id/rating` — 👍/👎 の付与・解除
- `GET/POST/PUT/DELETE /api/presets` — プリセット管理(2026-08-08: 各プリセットに評価集計 `upCount` / `downCount` を付与)
- ~~`GET /api/profile`~~ — 現行プロファイルの閲覧(2026-08-08 廃止)
- `GET/PUT /api/settings` — 設定(毎日の自動生成+外部コンテキスト+リアルワード制限)
- `GET /api/real-world-words` — 直近ウィンドウ内のリアルワード使用状況(パラメータ一覧ページ用)
- `GET /api/generation-params`(2026-08-09 追加)— おまかせ生成が LLM に注入する入力の一覧(冒険日確率・コンテキスト ON/OFF・評価集計付き要素プール・直近スタイル(和訳付き)・リアルワード制限の禁止/残り 1 回)。iOS の生成パラメータ画面用。runDaily と同じ関数群から組み立てることで表示と実際の生成入力のずれを防ぐ
- `POST /api/daily/run` — 自動生成の手動トリガ(検証・デバッグ用)
- `POST /api/generate` 拡張 — プリセット選択+自由テキストを受け、LLM 経由で生成。2026-08-09 追加: `artistSongId` を指定するとアーティスト経由生成(`mode = 'artist'`)になり、自由テキストは「追加の要望」として併用、`presetIds` は無視される
- `GET /api/artists/search`・`GET|POST /api/artists`・`GET /api/artists/:id/songs`・`POST /api/artists/:id/refresh`・`DELETE /api/artists/:id`(2026-08-09 追加)— アーティスト管理。登録は iTunes からの楽曲取り込みまで行い、`refresh` は差分のみ追加、削除はアーティストと曲を消す(生成済みタスクはスナップショットで表示継続)

## コスト見込み

- Suno(kie.ai): 12 クレジット/日 ≈ 月 360 クレジット(数百円以下)
- Claude API(Sonnet 5): 1 コール/日、数円/日程度(2026-08-08 にプロファイル更新コールを廃止して 2 → 1 に)
- `.env` に `ANTHROPIC_API_KEY` を追加

## 補足

- Suno 連携の抽象化(`SunoClient`)は維持。`customMode` 対応で `GenerateParams` を拡張する
- 冒険日の曲も評価対象。👍 が付けばその要素の集計が上がり、以後の通常日に選ばれやすくなる(冒険日自体は評価集計に従わなくてよいと指示。2026-08-08 変更)
