# DONE

- [x] 2026-08-10 コミットは main に直接行うことを CLAUDE.md に明記 — 既定ブランチへの直接コミットを避ける一般的な作法により作業ブランチを切ってしまい、単独開発で履歴が main 一本のこのリポジトリでは早送りマージの手間が増えていた。「コミットのルール」節を新設し、作業ブランチを切らないことと、プラン作成と実装をコミット単位で分けること(既存の 488c910 → 0cceecb が前例)を書いた

- [x] 2026-08-10 管理画面にモデルの表示と effort の切り替えを入れる — 生成の頭脳が何なのかが画面から分からず、品質・コスト・時間を動かすつまみも無かった。設定ページに「曲の生成(LLM)」パネルを新設し、**LLM モデル(`claude-sonnet-5`)と Suno モデル(`V5`)を読み取り専用で表示**、**思考の深さ(`output_config.effort`)を `low`〜`max` の 5 段階で切り替え**られるようにした。モデルを表示のみにしたのは `.env`(本番は環境変数)が真実源だからで、DB 設定にすると二重管理になり、存在しないモデル名を画面から保存できてしまう。effort は `settings` の `llm_effort`(既定 `high` = API の既定と同じなので未設定の既存 DB でも挙動が変わらない)に置き、生成のたびに読むステートレス方式で次の生成から即反映。API は `GET/PUT /api/settings` へのキー追加のみ(`llmEffort` は 5 値の enum バリデーション、`llmModel`/`sunoModel` は PUT で受け付けない)で既存キーの形状は不変のため旧 iOS アプリのデコードは通る。`settings.js` は `data-field` を総なめする汎用実装なので `<select>` は JS 変更なしで動き、読み取り専用表示の `data-readonly-field` だけ数行足した。**着手前の想定が実測で 2 回覆った** — ①`max_tokens` は思考と本文の合算の上限で、effort=max の実測は 1 曲目 15,639 / 2 曲目 20,139 トークンと振れ幅が大きく、**旧上限 16,000 では 2 曲目が切れていた**(JSON が途中で終わり `JSON.parse` 失敗 → タスク `FAILED`)。1 曲目が残り 361 トークンで完走したのを見て「足りている」と誤判定しかけた ②その対策として 32,000 に上げたら**全生成が即 `FAILED`** になった — TypeScript SDK は `max_tokens` が **21,333**(`60分 × max_tokens ÷ 128000 > 10分`)を超える非ストリーミング要求を「10 分を超える恐れ」として**送信前に例外**にする(クライアントに明示 `timeout` があるときだけ回避される)。プランに書いた「SDK がタイムアウトを自動で伸ばす」は誤り。→ 上限は effort によらず 32,000 の一本にし、呼び出しを**ストリーミング(`messages.stream().finalMessage()`)に変更**した。応答の扱いは非ストリーミングと同じで、`web_search` の server tool ブロックも同様に積まれる。あわせて生成のたびにモデル・effort・`stop_reason`・入出力トークン数をログに出し、上限に対する余裕が運用中に見えるようにした。検証: typecheck + 実 API への疎通(5 段階すべて HTTP 200・構造化出力と併用可)+ 隔離 DB サーバー(ポート 3015)で GET の既定値 `high`・5 値の保存・不正値 400・`llmModel` は PUT で変わらない・既存キーの回帰なし + 実生成 3 曲(effort=max の非ストリーミング 16000 で COMPLETE / effort=max のストリーミング 32000 で出力 20,139 トークンを完走して COMPLETE / 参照曲「夜に駆ける」+ effort=high でツール呼び出し 10 回・入力 141,514 トークンを完走し sources 4 件・固有名詞の混入なし)+ 管理画面をヘッドレス Chrome の CDP 操作で確認(初期表示にモデルと保存済み effort が出る → セレクト操作で「保存しました」→ リロードで保持)。iOS は不変更(API はキー追加のみなので影響なし)。なお参照曲ありの 1 曲は Suno 側で `style` 内の `major7`(コード名)を「アーティスト名」と誤判定されて弾かれた — 2026-08-09 に 1 度観測済みの既知事象がこれで 2 回目になった(本件とは無関係。対処は未実施)

- [x] 2026-08-10 web_search によるプロンプト生成と生成の非同期化の本番反映 — サーバーの本番(music.chobi.me)デプロイと実機アプリのインストール・操作確認をユーザーが実施してクローズ。確認(読み取りのみ): `https://music.chobi.me/health` 200 ok + `/api/tasks` の応答に `sources` キーがある(= 新サーバー)+ **実機から生成した task 17 が `mode: artist` で `sources` 5 件を持って COMPLETE** — リファレンスは YOASOBI「Seventeen」で、style に `140 BPM, C major` と**検索で確認した実測値が入り**、出典は songbpm.com と Wikipedia の 5 件、intent は根拠を書き分けている(「songbpm.com の実測データを検索で確認」/「楽曲を知っている前提で作風から具体化」/「原作のテーマ方向性は検索で確認」)。style / title / 歌詞に**固有名詞の混入なし**。あわせて **daily の 3 曲(task 14〜16)は `sources` 0 件**で、参照曲がない生成では検索が走らないという設計どおりの条件分岐が本番でも効いていることを確認した。生成の非同期化も本番のエッジ(Cloudflare)越しに動いており、192〜255 秒かかる参照曲ありの生成がタイムアウトせず完走している

- [x] 2026-08-09 プロンプト生成で web_search を使って外部情報を積極的に参照する — 参照曲について LLM に渡していたのは iTunes から取った 5 項目(アーティスト・曲名・アルバム・リリース年・ジャンル)だけで、曲を知らなければ作風から推定させていた。**問題は「知らない」ことではなく「知っているつもりで外す」こと** — 同じ参照曲(OFFICIAL HIGE DANDISM「Pretender」)で検索なしの生成は `138 BPM shuffle groove` と書いたうえで intent に「この曲を知っている前提で踏襲した」と申告した。実際は約 92 BPM で、ユーザーが intent を読んでも誤りに気づけない。Claude API の `web_search_20260209` サーバーツールで実際に調べさせる方式に切り替えた。**検索を有効にする条件はモード名ではなく `referenceSong` の有無**にした — 調べる対象があるのは実在の曲を指定されたときだけで、daily はニュース文脈が、manual はユーザーの自由記述が入力なので検索先が無い。この判定にしておくと TODO の「毎日作成を曲から選択」が入ったときも自動的に検索が効く。**着手前に実測して設計を決めた** — ①`output_config.format`(構造化出力)と server tool は**併用できる**(`stop_reason: end_turn` で JSON パースも成功)②本番と同じスキーマで **192 秒 / 入力 123,899・出力 11,086 トークン / 検索 5 回**(従来は 44 秒・入力約 700 トークン)で 1 曲約 $0.36 = 従来の約 9 倍 ③応答に `code_execution_tool_result` が混ざるがこれは動的フィルタリングが内部で使うもので `code_execution` を別途宣言してはいけない ④`pause_turn` は出なかったが内部で server tool を 12〜14 回呼ぶので `max_uses` 次第で起こりうる ⑤検索結果に固有名詞が大量に入るのに style / title / lyrics には混入しなかった。**この 192 秒が最大の設計要因**で、本番はエッジの Cloudflare が約 100 秒で切るため同期 API のままでは必ずタイムアウトする → **生成を非同期化した**(`POST /api/generate` は `status = 'PLANNING'` のタスク行を作って即返し、LLM → Suno はバックグラウンド。失敗は必ずタスクの `FAILED` として記録するので投げっぱなしでもエラーが進行状況に出る)。**DB スキーマ変更は最小** — `provider_task_id` が `TEXT NOT NULL` なので送信前は空文字列 + `PLANNING` で区別し、ポーラーの `listActiveTasks()` から除外して `getTask("")` を叩かせない。起動時に `PLANNING` のまま残ったタスクは再開できないので `FAILED` にする。スケジューラ(`runDaily`)はサーバー内で動きタイムアウト制約が無いので同期のまま(`acceptGeneration` → `await completeGeneration` に置き換えただけ)。ほかに `pause_turn` の再開ループ(「続けて」等の追加メッセージは付けない)、検索失敗は例外にならず `web_search_tool_result.content` がエラーオブジェクトで返るので警告ログのみで続行、プロンプトに「渡したアーティスト名・アルバム・リリース年と一致する曲か必ず確かめる」(同名異曲対策)と「intent に根拠を項目ごとに書き分ける」を追加、`SONG_PLAN_SCHEMA` に `sources` を足して `tasks.llm_sources`(JSON、`addColumnIfMissing` で後方互換)に保存し管理画面と iOS の楽曲詳細に「参照した情報源」として表示。`properNounsIn()` の固有名詞チェックと 1 回だけの再生成は**そのまま維持**(検索結果に固有名詞が入るぶんリスクは上がるので運用で観測する)。API は `status` の値が 1 つ増えるだけで**キーの形状は不変**のため旧アプリでもデコードは通る(ラベルが英語のまま出る)。検証: typecheck + 隔離 DB サーバー(`sqlite3 .backup` + `daily_enabled=false`)で、受付が **0.005 秒**で `PLANNING` を返すこと、`PLANNING` → `PENDING` → `TEXT_SUCCESS` → `FIRST_SUCCESS` → `COMPLETE` と遷移すること、LLM 失敗がタスクの `FAILED` + `error` に残ること(無効な API キーで再現)、ポーラーが `PLANNING` を叩かないこと、再起動で `PLANNING` が `FAILED` になること、参照曲なしの生成は `sources: 0` 件で検索ログも出ないこと + **通しの実生成 2 曲**(参照曲あり / なし)で、参照曲ありは `92 BPM felt as double-time 184 pulse, key of A-flat major` と**検索で確認した事実が style に入り**、`sources` に songbpm.com の URL 2 件、intent が「songbpm.com の検索結果で確認」と「この曲を知っている記憶に基づく」を書き分け、固有名詞の混入が無いことを確認 + 管理画面をヘッドレス Chrome の CDP 操作で目視(進行中カードの「曲を考えています…」、楽曲詳細の「参照した情報源」)+ シミュレータビルドと UI テスト 9 件成功。**想定と違ったこと**: 実生成は 192 秒ではなく約 255 秒かかった(server tool の呼び出しが 20 回で、検索の深さは実行ごとにぶれる)。また `ScreenshotUITests` が 1 度失敗した — 楽曲詳細に「参照した情報源」を足して画面が縦に伸びたことで `app.swipeUp()` 1 回では `detail.prompt` がミニプレイヤーの下に隠れたまま残り、そこへの `tap()` が座標としてミニプレイヤーに当たってフルプレイヤーのシートが開き以降の操作を吸っていた。`git stash` で変更を外すと成功することを確認して製品側ではなくテストの前提の脆さと切り分け、押せるようになるまでスクロールする形に直した。プラン: [docs/plans/archive/websearch-prompt-generation.md](docs/plans/archive/websearch-prompt-generation.md)

- [x] 2026-08-09 曲名からの登録の本番反映 — サーバーの本番(music.chobi.me)デプロイと実機アプリのインストール・操作確認をユーザーが実施してクローズ。API は追加のみで既存キーの形状が不変のため旧アプリ×新サーバーは完全互換だが、アプリの「曲名から生成」を使うには実機の再インストールが要るため両者を実施。確認(読み取りのみ): `https://music.chobi.me/health` 200 ok + `/api/ping` ok + **新エンドポイント `GET /api/artist-songs/search?term=Lemon` が本番で候補 23 件を返す**(先頭が `Lemon / 米津玄師`、trackId 1537460612)= 曲名検索とアーティスト逆引きのコードが本番で動作 + デプロイ後に `mode: artist` の task 13 が COMPLETE(参照曲 YOASOBI「三原色」→ 生成曲「Colors We Become」、error なし)。これで参照曲の登録経路 2 つ(アーティスト名から / 曲名から)がどちらも本番稼働

- [x] 2026-08-09 曲名を登録できるようにする(曲名からアーティストも逆引き) — アーティスト経由生成の入口がアーティスト名の検索しか無く、「この曲みたいな曲を作りたい」ときに **アーティスト名を思い出す → 登録 → 最大 200 曲から目当ての曲を探す** という遠回りを強いていた(アーティスト名が分からない曲はそもそも入口に立てない)ため、曲名で検索してその曲を直接登録できる入口を足した。曲を選んだ時点で iTunes の応答からアーティストを逆引きできるので、アーティストの登録もまとめて済ませる。**逆引きは曲検索(`entity=song`)の track 行に `artistId` が入るので検索の 1 リクエストで完結する**。設計の肝は **登録名を `lookup` の artist 行の正式表記から採る**こと — `country=JP` の曲検索が返す track 行の `artistName` はローカライズされた表示名(`artistId` 3296287 が「クイーン」、530814268 が「米津玄師」、1459216694 が「Official髭男dism」)で、これを使うと ①既存のアーティスト名検索経路と名前が食い違い `artists.name` の UNIQUE が効かず二重登録になる ②`llm.ts` の固有名詞混入チェック(`containsName(body, ref.artist)`)は `artists.name` を**英語の** style / title / lyrics から探すため「クイーン」で登録すると "Queen" の混入を検出できなくなる。カバー・ピアノ版・オルゴールのノイズが多い(「Lemon」の上位 10 件で本家の米津玄師は 2 行だけ)ためアーティスト名検索と同じく候補一覧から選ばせ(`limit` 25)、同一アーティストの同一曲名が複数版返る(クイーンの Bohemian Rhapsody が 4 行)ので `(artistId, 正規化した曲名)` で既存 `fetchSongs()` と同じ規則(trim + 小文字化・最古を残す)の重複除去を掛ける(実測 25 → 18 件)。日本語で検索できるが読みマッチで曲名の違う行も混ざるので候補には曲名とアーティスト名の両方を出す。コラボ曲の `artistId` は片方のもの(「打上花火 / DAOKO×米津玄師」→ Daoko)なので、登録先が表示名と違う場合は登録後に明示する。`lookup` は 200 件で頭打ちで選んだ曲が漏れうるため**選んだトラックは必ず個別に `INSERT`** する(大小文字だけ違う重複行ができないよう正規化して既存行を探してから足す — `artist_songs` の UNIQUE は BINARY 照合のため)。**既存を汚さない方向に寄せた** — DB スキーマ変更なし・生成ロジック(`llm.ts` / `generation.ts` / `scheduler.ts`)不変更で、`artist_songs` に行を足す入口が増えるだけ(生成は既存の `POST /api/generate { artistSongId }` をそのまま使う)。API は `GET /api/artist-songs/search` と `POST /api/artist-songs` の追加のみで既存キーの形状を変えないため旧アプリ×新サーバーは完全互換。`POST` の body は `{ itunesTrackId }` だけにしてクライアントの申告は信用せずサーバーが `lookup` で取り直し、アーティストが登録済みなら追加の `lookup` を打たない。曲名から登録する場合もアーティストの曲を全部(最大 200 曲)取り込む — 正式名とジャンルを取るために `lookup` はどのみち 1 回必要で、曲一覧は同じ応答に入っており追加コストがゼロのため。**iOS の導線は「生成タブに専用の行を新設」に決定**(プラン原案のアーティスト追加シート内のセグメント切り替えは、曲名しか知らない人が「アーティスト」を 2 回通ることになり動機と噛み合わないため取り下げ) — `SongSearchView` を新設し、候補をタップすると登録と生成を続けて実行して生成タブへ戻る(生成だけ失敗しても登録は済んでいる旨をメッセージに出す)。管理画面は画面を増やさず登録パネル内の切り替え(`<select>`)にし、登録後はその曲で絞り込んだ曲一覧を開いて「この曲で生成」がすぐ押せる状態にする。検証: typecheck + 隔離 DB サーバー(`sqlite3 .backup` + `daily_enabled=false`、ポート 3015)への curl で実 iTunes 通信込みの一連 — 曲名検索(日本語「レモン」/ 英語「Bohemian Rhapsody」で重複除去 25 → 18 / 0 件 / term 未指定 400)、未登録アーティストの曲を登録して正式表記 `Kenshi Yonezu` + 130 曲 + 対象曲を含むこと、登録済みアーティストの別の曲は `artistCreated: false` で曲追加なし、200 件から漏れた曲の個別 INSERT(対象曲を DELETE してから再 POST し 1 曲だけ増える)、コラボ曲が `Daoko` として登録されること、大小文字違い(`LEMON` に書き換えてから `Lemon` を登録)が既存行に合流して重複行ができないこと、存在しない `itunesTrackId` は 404 で DB が変わらないこと、`/api/*` が X-API-Secret 必須(無しは 401)、既存経路(`POST /api/artists` / `refresh`)の回帰なし + **通しの実生成 1 曲**(曲名「Pretender」→ `OFFICIAL HIGE DANDISM` を逆引き登録 → 生成)で COMPLETE(3:09、`Almost Yours`)・style / title / 歌詞に固有名詞なし・`ref_artist_name` / `ref_song_title` のスナップショット記録を確認 + 管理画面をヘッドレス Chrome の CDP 操作で曲名モード → 検索 → 登録 → 曲一覧が絞り込み済みで開くところまで目視(**長い曲名が副題を 1 文字幅に潰すレイアウト崩れを発見し `style.css` を修正** — `.artist-name` / `.song-title` に `flex: 1 1 auto`、行末ボタンに `flex: 0 0 auto`)+ シミュレータビルドと UI テスト 9 件成功(`GenerateUITests` に `testSongSearchEntryPoint` を追加、既存に回帰なし)。**別件の発見**: 同じ参照曲でもう 1 曲生成したところ Suno が style 内の `major7`(コード名)を「アーティスト名」と誤判定して `SENSITIVE_WORD_ERROR` で弾いた。artist モードはコード進行を音楽用語で書かせるため一定確率で起こりうるが、再現 1 回のみのため未対処(頻発するなら `llm.ts` の指示でコード名の書き方を制限する)。プラン: [docs/plans/archive/song-title-registration.md](docs/plans/archive/song-title-registration.md)

- [x] 2026-08-09 アーティスト経由生成の本番反映 — サーバーの本番(music.chobi.me)デプロイと実機アプリのインストール・操作確認を実施してクローズ。API は追加のみで既存キーの形状が不変のため旧アプリ×新サーバーは完全互換だが、アプリからアーティストを登録・生成するには実機の再インストールが要るため両者を実施。確認: `https://music.chobi.me/health` 200 ok + 本番 API(X-API-Secret)の `/api/artists` に実機から登録した YOASOBI(itunesArtistId 1490256993 / J-Pop / 78 曲)が入っていること = iTunes からの楽曲取り込みが本番でも動作 + `/api/tasks` の task 12 が `mode: artist` で COMPLETE(参照曲 YOASOBI「UNDEAD」→ 生成曲「Still I Rise」、error なし)= **本番の Suno でも `SENSITIVE_WORD_ERROR` にならず通しで生成できることを実機経由で確認**。これで生成経路 3 つ(おまかせ / カスタム / アーティスト経由)がすべて本番稼働

- [x] 2026-08-09 音楽生成の経路アーティスト経由を追加 — 好きなアーティストの特定の曲に似た曲を作る 3 つ目の生成経路(`mode = 'artist'`)。アーティスト登録 → iTunes からの楽曲自動取り込み → 曲を選んで LLM がプロンプトを書き Suno で生成、という流れ。**楽曲データソースは iTunes Search API**(キー不要・無料で `.env` も本番のシークレット管理も増えない)。実測どおり日本語「米津玄師」でヒットするが返る名前はローマ字「Kenshi Yonezu」で別名義も混ざるため候補一覧から選ばせ、`lookup` の上限 200 件・同名の重複は曲名正規化で最古を残す除去(実測 162 track → ユニーク 130 曲)を `itunes.ts` に閉じ込めた。**この機能の肝は Suno に固有名詞を渡さないこと** — Suno はアーティスト名・曲名をモデレーションで弾く(`SENSITIVE_WORD_ERROR`)ため、LLM には参照曲の音楽的特徴(ジャンル・テンポ・リズム・コード進行の雰囲気・楽器編成・ボーカルの質感・プロダクション)を英語の音楽用語へ翻訳させ、固有名詞を落とした style を書かせる+送信前にサーバーで混入を検査(style/title/lyrics のアーティスト名、title の曲名。ASCII は語境界で見て「Air」が「airplane」に誤ヒットしないようにし、曲名は普通の単語のことがあるので歌詞は見ない)して禁止ワードと同じ 1 回だけの再生成にまとめた。歌詞は原曲の複製を明示的に禁止し、LLM が曲を知らない場合は作風からの推定を許したうえで根拠を `intent` に書かせる。既存の学習ループは汚さない — artist は要素プールを提示しないので 👍/👎 はプリセット集計に入らず、直近スタイルの重複回避は「参照曲に似せる」目的と矛盾するので注入せず、歌声も「幅広い声色を試す」から「参照曲の質感に寄せる」に差し替え。daily / manual のロジックは分岐追加のみで不変更。サーバー(`itunes.ts` 新設+`artists`/`artist_songs` テーブル+`tasks` に `artist_id`/`artist_song_id`/`ref_artist_name`/`ref_song_title`+アーティスト管理 API 6 本+`POST /api/generate` の `artistSongId`)、管理画面(`artists.html`/`artists.js` 新設+全ページのサイドバーに導線+楽曲詳細に「リファレンス」+進行中カードのモードバッジを表引きに修正)、iOS(`ArtistsView`/`ArtistSongsView` 新設+生成タブの導線+楽曲詳細のリファレンス表示+`BackendAPI.delete` と POST の timeout 指定を追加)。**API は追加のみで既存キーの形状は不変**のため旧アプリ×新サーバーは完全互換(本番反映はサーバーデプロイのみ必須 — 新規 TODO として残した)。検証: typecheck+隔離 DB サーバー(`sqlite3 .backup`+`daily_enabled=false`、ポート 3015)への curl で実 iTunes API 込みの一連(邦楽は候補選択・洋楽は名前のみで最上位候補に解決 → 登録 130/180 曲・重複除去が効くこと/同名 409/存在しないアーティスト 404/`refresh` が差分だけ足すこと(0 件 → 1 件消して 1 件)/削除で曲も消えること/不正な `artistSongId` は LLM を呼ばず 404)+使い捨てスクリプトで `buildSongPlanPrompt()` の artist 分岐と固有名詞チェックを 24 項目直接検証(要素プール・直近スタイルの非注入と、manual/daily に影響が無いことを含む)+管理画面をヘッドレス Chrome を CDP で操作して検索 → 曲一覧 → 絞り込みまで目視+シミュレータビルドと UI テスト(`GenerateUITests` にアーティスト導線のスモークを追加。曲一覧・追加シート・生成確認ダイアログは隔離サーバーへ向けた使い捨てテストでスクショ目視 — `.searchable` がこの画面構成では出ないと分かり通常の絞り込み入力欄に差し替えた)。**通しの実生成 1 曲**(Kenshi Yonezu「Lemon」を参照)も実行し、Suno が `SENSITIVE_WORD_ERROR` を返さず COMPLETE(3:24)になること・style / title / 歌詞に固有名詞が無いこと(「Persimmon Rain」— レモンを柿に置き換えた新作歌詞)・`intent` に根拠が書かれること・アーティストを削除しても楽曲詳細のリファレンス表示がスナップショットで残ることを確認。プラン: [docs/plans/archive/artist-based-generation.md](docs/plans/archive/artist-based-generation.md)

- [x] 2026-08-09 アプリで曲生成でカスタム指定をするときにキーボードが消せない — カスタムプロンプト入力欄は複数行 TextField(`axis: .vertical`)でリターンキーが改行になるため閉じる手段が無かった。`GenerateView` に `@FocusState` を追加し、キーボードツールバー(`ToolbarItemGroup(placement: .keyboard)`)の「閉じる」ボタンでフォーカスを外す+ScrollView に `.scrollDismissesKeyboard(.interactively)`(下スワイプでも閉じる)+「生成する」送信時にもフォーカスを外して送信後にキーボードが残らないようにした。他の入力欄も点検 — 設定タブのタイムゾーン(`.submitLabel(.done)` + `.onSubmit`)・サーバー URL・API Secret は単一行で return で閉じるため問題なし。一貫性のため `SettingsView` の ScrollView にもスワイプ dismiss だけ追加(タイムゾーン欄をスワイプで閉じた場合は保存されないが「return = 確定保存」の既存設計どおりキャンセル相当)。iOS のみの変更でサーバー・API・管理画面は不変更。検証: シミュレータビルド+`GenerateUITests` にキーボード閉じの検証を追加(typeText → 閉じるタップ → キーボード消滅。ハードウェアキーボード接続時はソフトウェアキーボードが出ないため表示を前提条件にガード+開閉のスクリーンショットを xcresult に添付して目視確認)+`SettingsUITests` で回帰なし(いずれも隔離 DB サーバー: sqlite3 .backup+daily_enabled=false、ポート 3014)。プラン: [docs/plans/archive/keyboard-dismiss.md](docs/plans/archive/keyboard-dismiss.md)

- [x] 2026-08-09 プリセット楽器カテゴリ削除の本番反映 — サーバーの本番(music.chobi.me)デプロイ(5195573)と動作確認を実施してクローズ。API 不変更(presets 応答から instrument 行と categoryLabels の instrument キーが消えるだけ)で旧アプリ×新サーバーは完全互換のため、サーバーデプロイのみでクローズ — 実機アプリの再インストールは不要。確認: `https://music.chobi.me/health` 200 ok+本番 API(X-API-Secret)の `/api/generation-params` で presets が genre 12 / mood 10 / tempo 4 / vocal 20 の 46 件のみ・categoryLabels からも instrument が消えた = 起動時の一回限りマイグレーションが本番 DB の instrument プリセット 11 件を削除済み。以後の毎日の自動生成から要素プールに楽器が提示されず、楽器は LLM が style 内でジャンルに合わせて自由に書く

- [x] 2026-08-09 プリセットから楽器を削除するのを検討 — 検討の結果、楽器(instrument)カテゴリを廃止。本番データ(API 経由)で判断: usedPresets が記録されている直近 2 曲はどちらも楽器プリセットを 3〜4 個採用し「acoustic folk + rock に synthesizer」などジャンルの編成と喧嘩する組み合わせが実際に出ている一方、楽器プリセットが無い曲でも LLM は style 内に「solo acoustic grand piano」等の具体的な楽器を自前で書いており楽器の具体性はプールに依存しない。さらにカテゴリが減るほど 1 曲の 👍/👎 の帰属がシャープになり、楽器プリセットの評価集計は全て 0 でいま失うものが無い(楽器単位の好み学習が将来必要になれば音源解析(Gemini)タスクで実測ベースの再導入を検討 — TODO に注記済み)。実装はサーバーのみ: `presets.ts` のシード 11 件+`CATEGORY_LABELS` の instrument を削除し、既存 DB 向けに起動時の一回限りマイグレーション(`db.deletePresetsByCategory()` 新設。実施済みを `settings` の `migration_drop_instrument_presets` に記録し、ユーザーが後から手動で再追加したものは再起動で消さない。`task_presets` のスナップショット・評価履歴は設計どおり残置)。`llm.ts` の style スキーマ説明「ジャンル・楽器・…」は維持(楽器は LLM がジャンルに合わせて自由に書く)。管理画面・iOS はカテゴリを API 応答から動的に組み立てているため不変更で、API 形状も不変更 — 旧アプリ×新サーバーは完全互換(本番反映はサーバーデプロイのみ・TODO 残し)。spec 2 本(決定の記録は日付付き追記、flow は入力表のカテゴリ列挙を書き換え。SVG 3 枚と CLAUDE.md はカテゴリ列挙が無く不変更)も更新。検証: typecheck+隔離 DB(DB_PATH 指定)で 3 シナリオ — フレッシュ DB はシード 46 件(instrument 無し)+フラグ記録/instrument 入り DB(本番相当)は初回起動で全削除+他カテゴリ不変+再シード無し/フラグ後の手動再追加は再起動で残置 — +settings API にフラグが漏れないことを curl 確認+使い捨てスクリプトで `buildSongPlanPrompt()` に instrument 行が入らないことを直接検証(実生成なし)+管理画面プリセットページをヘッドレス Chrome 目視。プラン: [docs/plans/archive/remove-instrument-presets.md](docs/plans/archive/remove-instrument-presets.md)

- [x] 2026-08-09 毎日の自動生成 3 曲の本番反映 — サーバーの本番(music.chobi.me)デプロイ(5e9ccbd。g3plus-ops 側の 427a64f で実施・記録済み)を確認してクローズ。API は既存キー不変更+settings への `dailyCount` 追加のみで旧アプリ×新サーバーは完全互換のため、今回はサーバーデプロイのみでクローズ — 実機アプリの再インストール(`./run-ios-device.sh`)は設定タブで「1 日の曲数」を変更したくなったときでよい(新アプリ×旧サーバーは設定タブがデコードエラーになるため、その際もサーバーが先行済みなので問題なし)。確認: サーバー上の clone が 5e9ccbd・コンテナ起動ログ正常・`/health` ok・サーバー内 curl の `/admin/api/settings` に `dailyCount: 3`(`lastDailyDate` が当日なのでデプロイ当日の追い生成も無し — `last_daily_count` の無い旧データはその日を生成済み扱いにする設計どおり)+外形の `https://music.chobi.me/health` 200 と `/admin/` 302(Cloudflare Access)。明日の PT 6:00 から 1 日 3 曲が 1 曲ずつ順次生成される

- [x] 2026-08-09 毎日の自動生成で３曲生成 — 1 日の曲数を新設定 `daily_count`(既定 3、1〜10)にし、スケジューラが残数分だけ `runDaily()`(1 回 = 1 曲)を順次実行する方式。成功のたびにその日の生成済み数を `last_daily_count` へ記録し、途中失敗(30 分後に再試行)・サーバー再起動・停止中の跨ぎのいずれも残数だけ追い生成する(重複生成しない。`last_daily_count` の無い旧データはその日を生成済み扱いにしてデプロイ当日の追い生成を防止)。`runDaily()` と `POST /api/daily/run` は不変更 — 本番はエッジが Cloudflare のため同期 API で 3 曲分の LLM 生成を待つとタイムアウトする上、API 不変更で旧アプリ×新サーバー完全互換になる(おまかせ生成ボタンは従来どおり 1 タップ 1 曲)。冒険判定は従来どおり `runDaily()` 内で曲ごと(「冒険日」の UI 文言は維持し spec に注記)。順次実行なので 2 曲目以降のプロンプトには同日の前の曲のスタイル・リアルワードが既存の仕組みで注入され曲間の重複を避けられる(直近スタイルの注入は 5 → 10 件に拡大)。設定 UI は `PUT /api/settings` に `dailyCount`(1〜10 バリデーション)+管理画面 settings.html+iOS 設定タブ(曲数 Picker)。spec 2 本(決定の記録は日付付き追記、flow は手順・入口表・入力表を書き換え。SVG 2 枚は曲数の記載が無く不変更)と CLAUDE.md も更新。検証: typecheck+使い捨てスクリプトで `shouldRunDaily()` の残数ロジック 8 ケース(途中失敗からの再開・旧データ互換・曲数の増減)を直接検証(実生成なし)+隔離 DB(DB_PATH 指定のフレッシュ DB)サーバーへの curl(`dailyCount` の GET/PUT 反映と範囲外 400)+管理画面設定ページをヘッドレス Chrome 目視+シミュレータビルド+SettingsUITests / ScreenshotUITests(設定タブの曲数行を目視)。プラン: [docs/plans/archive/daily-three-songs.md](docs/plans/archive/daily-three-songs.md)

- [x] 2026-08-09 ニュースソース変更(英語メディア)の本番反映 — サーバーを本番(music.chobi.me)へデプロイ(1c3bf94)。API 不変更のため旧アプリ×新サーバーは完全互換で、今回はサーバーデプロイのみでクローズ(実機アプリの再インストールは設定タブのヒント文言が古いだけなので次の機会でよい)。以後の毎日の自動生成から「今日のコンテキスト」が Google News RSS 米国版(英語)の見出しになる

- [x] 2026-08-09 ニュースのソースを海外の英語メディアに変更 — 毎日の自動生成の「今日のコンテキスト」に注入するニュースを Google News RSS 日本版から米国版(英語)`hl=en-US&gl=US&ceid=US:en` へ切り替え。歌詞は英語で生成するため、海外英語メディア(Reuters・The Guardian 等の見出しが集約されて返る)の方がテーマ語(リアルワード)や雰囲気が英語歌詞に自然に馴染む。XML 構造が日本版と同一のためコード変更は `context.ts` の URL 定数+コメントのみ(パーサ・件数上限 8 件・失敗時スキップ・設定キー `context_news` は不変更)。BBC 等の単一媒体 RSS は編集方針の偏り+CDATA 形式でパーサ調整が要るため不採用。ほかは文言(管理画面 settings.html と iOS 設定タブのヒント「日本版」→「米国版(英語)」)と spec 2 本(決定の記録は日付付き追記、flow は入力表を書き換え。SVG と CLAUDE.md はロケール記述が無く不変更)。API 不変更のため旧アプリ×新サーバーは完全互換 — 本番反映はサーバーデプロイのみ必須(TODO 残し)。検証: typecheck+隔離 DB(DB_PATH 指定のフレッシュ DB)で `buildTodayContext()` を直接呼ぶ使い捨てスクリプトを実行し英語見出し 8 件の整形を確認(daily 実生成なし)+隔離 DB サーバー(daily_enabled=false)で管理画面設定ページをヘッドレス Chrome 目視+シミュレータビルド+ScreenshotUITests で設定タブの文言を目視。プラン: [docs/plans/archive/news-source-english-media.md](docs/plans/archive/news-source-english-media.md)

- [x] 2026-08-09 生成時の天気削除の本番反映 — サーバーを本番(music.chobi.me)へデプロイ(2d4b2c9)し、実機アプリを再インストール(`./run-ios-device.sh`)。旧アプリは新サーバー応答で設定タブ・生成パラメータ画面がエラー表示になるため両者をセットで実施。本番の settings / generation-params 応答から weather 系キーが消えたことをサーバー内 curl で確認済み

- [x] 2026-08-09 生成時の天気を削除 — 毎日の自動生成の「今日のコンテキスト」から天気(Open-Meteo)を削除し、ニュースのみに一本化。サーバーは `context.ts` の天気ソースと weather 系設定(`context_weather` / `weather_city` / `weather_lat` / `weather_lon`)の読み書き・`PUT /api/settings` のバリデーション・`GET /api/generation-params` の contextWeather / weatherCity を削除(DB の既存 weather 系 settings 行は参照されない孤児として残置 — 廃止済み profile テーブルと同じ扱い)。管理画面設定ページの天気トグル・都市選択と、iOS の設定タブの天気トグル・都市ピッカー(47 都道府県リスト)・生成パラメータ画面の天気表示・生成タブ説明文も削除。旧アプリ×新サーバーは設定タブ・生成パラメータ画面のデコードエラー表示のみで他機能に影響しないが、**本番反映はサーバーデプロイ+実機再インストールをセットで行う(TODO 残し)**。spec 2 本(決定の記録は日付付き追記、flow は挙動書き換え+SVG 2 枚)と CLAUDE.md も更新。検証: typecheck+隔離 DB サーバーへの curl(settings / generation-params から weather 系が消え、weather キーのみの PUT は 400)+管理画面ヘッドレス Chrome+シミュレータビルド+SettingsUITests / ScreenshotUITests(設定・生成・生成パラメータ画面を目視)。プラン: [docs/plans/archive/remove-weather-context.md](docs/plans/archive/remove-weather-context.md)

- [x] 2026-08-09 生成パラメータ表示の本番反映 — サーバーを本番(music.chobi.me)へデプロイし、実機アプリを再インストール(`./run-ios-device.sh`)して実機で生成パラメータ画面の表示を確認。これで生成パラメータ表示タスク(下記)は本番でも稼働

- [x] 2026-08-09 アプリの生成タブに生成に使われるパラメータを表示する — おまかせ生成が LLM に注入する入力をアプリ単体で確認できるようにした(管理画面のパラメータ一覧の iOS 版)。サーバーは集約 API `GET /api/generation-params` を新設し、runDaily と同じ関数群(getDailySettings / getContextSettings / presetJson+countPresetRatings / currentWordLimits)から組み立てて表示と実際の生成入力のずれを防ぐ(冒険日確率・コンテキスト ON/OFF+都市・評価集計付き要素プール・直近スタイル(新設 `listRecentStyleRows()` で和訳付き)・禁止/残り 1 回ワード・追跡ワード数)。iOS は生成タブのヒーロー直下に「生成パラメータ」行(NavigationLink)を追加し、読み取り専用の `GenerationParamsView` を新設 — 生成の設定 3 行/カテゴリ別ピルの要素プール(評価があるものだけ 👍n/👎n 併記 = presetLines の suffix と同規則)/直近スタイル(styleJa 主・英語原文従、無い旧データは英語のみ)/リアルワード使用状況(使用禁止は強調ピル+追跡数フッター。全ワード一覧は管理画面の役割として持たない)。旧サーバー×新アプリは画面内エラー表示のみで他機能に影響なし。検証: typecheck+実 DB スナップショット(`sqlite3 .backup`。cp だと WAL 残留で別 DB になる罠を踏んだ)の隔離サーバー(daily_enabled=false にして意図しない実生成を防止)に ダミー task_presets を投入して curl で全フィールド確認+ScreenshotUITests に生成パラメータ 3 枚(トップ/中間/最下部)を追加しライト/ダークで目視。本番反映(サーバーデプロイ+実機再インストール)は TODO 残し。プラン: [docs/plans/archive/generate-tab-params.md](docs/plans/archive/generate-tab-params.md)

- [x] 2026-08-09 プリセット優先度タスクの本番反映 — サーバーを本番(music.chobi.me)へデプロイし、実機アプリを再インストール(`./run-ios-device.sh`)。旧 iOS ビルドは /api/daily/run 応答の profileUpdated(削除済み)を non-optional デコードしていたため両者をセットで実施。以後の毎日の自動生成から使用プリセットの記録(`task_presets`)が始まり、評価(👍/👎)が溜まるにつれプリセット別集計が生成プロンプトに効いていく

- [x] 2026-08-09 リアルワード: 外部リソース(ニュース・天気)から曲の着想ワードを作り、保存・使用制限で似た曲の連続生成を防ぐ — 全 5 Phase 実装済みでクローズ。①注入基盤+ニュース(Google News RSS)+パラメータ設定画面新設 ②リアルワードの生成・保存(`real_world_words` テーブル)+楽曲詳細に表示 ③使用回数の集計とプロンプトでの制限(直近 30 日で同一ワード 2 回まで。上限到達は「使用禁止ワード」、残り 1 回は「残り 1 回のワード」として注入し、ウィンドウを外れた語は自動で再利用可能に)+検証リトライ(禁止ワードが出力に残ったら 1 回だけ再生成)④天気(Open-Meteo)+設定画面に天気 ON/OFF・都市選択 ⑤パラメータ一覧ページに使用ワードと残り回数を表示。以後の毎日の自動生成で稼働中。残っていた子項目「稼働後チェック(Sonnet 5 の生成精度を確認し、悪ければ `.env` の `LLM_MODEL` を claude-opus-5 に変更)」は個別のタスクとしては持たずクローズに合わせて閉じた(気になったときに `.env` で切り替えれば足りる)。プラン: [docs/plans/archive/real-world-words.md](docs/plans/archive/real-world-words.md)

- [x] 2026-08-08 使用プリセットの保存と評価による優先度付け+プロファイル文書の廃止 — 全 5 Phase 完了。①使用プリセットの記録: LLM の構造化出力に `usedPresets` を追加し、サーバーで `presets` と照合して新設の `task_presets` テーブル(preset_id +使用時点のスナップショット)にタスク単位で保存(manual はユーザー選択との和集合)②評価集計の優先度付け: 生成のたびに task_presets × tracks.rating でプリセット別 👍/👎 を全期間集計し(ステートレス、評価変更が即反映)、要素プール行に併記して daily は 👍 優先・👎 回避(1 要素は外すルール維持)、冒険日は従わなくてよい、manual は非注入。`GET /api/presets` に upCount/downCount、presets 管理ページに集計表示 ③プロファイル文書の廃止: 好みの管理を評価集計に一本化し、生成プロンプト注入・毎朝の更新 LLM コール(1 回減)・updateProfile・listRatedTracks・GET /api/profile・管理画面パネル・iOS profileUpdated を削除(profile テーブルと過去データは履歴として残す)④生成プロンプトの分かりやすい表示: llm_prompt を行頭「## 」でセクション分解して管理画面は小見出し+字下げ本文、iOS は Track に llmPrompt を optional 追加して楽曲詳細に折りたたみ整形表示(見出し無し旧データは全文フォールバック、無いデータは非表示)⑤iOS 楽曲詳細にリアルワードと同じピル形式で使用プリセット表示(UsedPreset を optional デコード)。spec 2 本(決定の記録は日付付き追記、flow は挙動書き換え+SVG 図 2 枚更新)と CLAUDE.md も更新。検証は typecheck+実 DB コピー(DB_PATH 隔離)での API・ヘッドレス Chrome・iOS は ScreenshotUITests ライト/ダーク目視。**本番反映はサーバーデプロイと実機再インストールをセットで行う(TODO 残し)**。プラン: [docs/plans/archive/preset-priority.md](docs/plans/archive/preset-priority.md)

- [x] 2026-08-08 specs に音楽生成の流れを書く(図も使ってわかりやすくする)— [docs/specs/music-generation-flow.md](docs/specs/music-generation-flow.md) を新設。既存の [music-generation.md](docs/specs/music-generation.md)(設計決定の記録)と役割分担し、こちらは現在の実装の挙動に徹して相互リンク。図は SVG 3 枚(①全体像: 3 つの入口 → LLM → Suno → 保存 → アプリ+評価のフィードバックループ ②毎日の自動生成の 5 手順 ③Suno タスクの状態遷移)を `docs/specs/music-generation-flow/` に置き画像参照で埋め込み — mermaid は vibeboard(marked)が描画できず、インライン SVG は GitHub が除去するため、両方で表示される唯一の方式。配色はアプリのデザイントークン(クリーム/オリーブ)に合わせ SVG 自体に背景を持たせダークテーマでも可読。本文は入口 3 つの比較表・LLM 入出力の表・リアルワード検証リトライ・状態遷移の補足・評価ループを収録し、コード(scheduler/llm/context/generation/kieai)から起こして検証。vibeboard の実ページをヘッドレス Chrome で撮り本文+図 3 枚の描画を目視確認(SSE でロードが終わらないため --timeout 指定が必要)。プラン: [docs/plans/archive/music-generation-flow-spec.md](docs/plans/archive/music-generation-flow-spec.md)

- [x] 2026-08-08 ロック画面・コントロールセンターの Now Playing 表示/操作を実機で確認する(iOS アプリデザイン Phase 5 の持ち越し)— `./run-ios-device.sh` での実機確認で問題なし。これで iOS アプリデザインタスクの残件はすべて完了

- [x] 2026-08-08 iOSアプリデザインをする(色味はアイコンに合わせる。画面レイアウト・機能も再考) — アイコン実測色のデザイントークン(AccentColor `#B4BA40` / AppBackground クリーム / AccentDeep 文字用)を Assets カラーセット+Theme.swift に定義し(Phase 0)、実データ入り HTML モック 3 案から**案A ミニマル**(フラット面・ヘアライン区切り・余白広め・アクセントは要所のみ)を選定(Phase 1)して全 9 Phase で実装。全画面のフラット化とティント適用(Phase 2)、ライブラリ刷新 — 今日の一曲ヒーロー・進行中ジョブカード・styleJa/リアルワードピルの行・日付グループ(Phase 3)、楽曲詳細画面新設 — 歌詞 EN/JA 切替・狙い・スタイル・メタ情報(Phase 4)、プレイヤー強化 — キュー連続再生・ロック画面 Now Playing・フルプレイヤーシート・ミニプレイヤー刷新(Phase 5)、生成タブのおまかせ生成(daily/run)主役化+カスタム生成折りたたみ+クレジットピル(Phase 6)、設定画面の構成統一+芝生イコライザの再生中アニメーション(Phase 7)、サーバー設定(自動生成・ニュース/天気・都市)の閲覧・編集を設定タブに追加 — 変更で即 PUT する管理画面と同じ操作感(Phase 8)。検証は各 Phase でシミュレータビルド+XCUITest スモーク+ライト/ダークのスクリーンショット目視(UI テストは Playback/Rating/TrackDetail/Player/Generate/Settings/Screenshot の 7 スイート構成に)。ロック画面の実機確認も 2026-08-08 に完了(上記エントリ)。プラン: [docs/plans/archive/ios-app-design.md](docs/plans/archive/ios-app-design.md)

- [x] 2026-08-08 アプリアイコンを変更する — 用意済みのアイコン画像(緑の音符+芝生風イコライザ、1254x1254 PNG)を 1024x1024 に縮小して `AppIcon.appiconset` に登録(iOS 17+ の単一サイズ形式。`Contents.json` に filename を追記、`project.yml` は `ASSETCATALOG_COMPILER_APPICON_NAME` 設定済みのため変更なし)。シミュレータビルド成功+ビルド成果物の `Assets.car` に 1024px アイコンが含まれること・派生 120px アイコンの見た目を確認。プラン: [docs/plans/archive/change-app-icon.md](docs/plans/archive/change-app-icon.md)

- [x] 2026-08-08 アプリ名の変更 — 新名称 **Music Plant**(「AI が毎日音楽を製造する無機質な工場」のイメージが自動生成に合うため。約 40 案から選定)。変更は表示名のみ: iOS ホーム画面表示名(`CFBundleDisplayName`)、管理画面 3 ページの `<title>`・サイドバー、起動ログ・README・CLAUDE.md。内部識別子(bundle id `com.akiraak.dailyaimusic`、Xcode プロジェクト名 `DailyAIMusic`、リポジトリ名、ドメイン `music.chobi.me`)は据え置き — bundle id 変更は実機で別アプリ扱いになり設定が消えるため。typecheck + シミュレータビルド+ヘッドレス Chrome で 3 ページのサイドバー表記を確認。プラン: [docs/plans/archive/rename-app.md](docs/plans/archive/rename-app.md)

- [x] 2026-08-07 歌手の特徴もパラメータ指定できるように — プリセットに新カテゴリ `vocal`(歌声)を追加し、性別 × 声質(クリア・ハスキー・パワフル・ウィスパー・ファルセット等)× 年齢感+アンサンブル・特殊系(デュエット・聖歌隊・子ども合唱・オペラ・ボコーダー)の初期セット 20 件で広い声色をカバー。起動時シーディングを「テーブルが空のとき全件」から「DB に 1 件も無いカテゴリだけ投入」に変更し、既存 DB にも vocal が入るように(既存カテゴリ内の編集・削除は復活しない)。LLM プロンプトの出力条件に「歌詞ありの場合は style に歌声の特徴を必ず含め、直近と偏らないよう幅広い声色を試す」を追加。typecheck +既存 DB への追加投入・再起動での重複なし・プロンプト出力・管理画面の歌声グループ表示(ヘッドレス Chrome)を確認。プラン: [docs/plans/archive/vocal-presets.md](docs/plans/archive/vocal-presets.md)

- [x] 2026-08-07 曲生成は２曲のうち１曲目のみを採用する — Suno は 1 リクエストで通常 2 曲返すが同一歌詞・スタイルのバリエーションで一覧にほぼ同じ曲が並ぶため、完了検知時に `sunoData` 先頭の 1 曲目のみダウンロード・DB 登録するように変更(`generation.ts` の `pollTask` で `slice(0, 1)`。クレジット消費はリクエスト単位の 12 で不変、既存の保存済みトラックはそのまま)。仕様書の 2 曲前提の記述も更新。typecheck のみ(実生成での確認は次回生成時)。プラン: [docs/plans/archive/adopt-first-track-only.md](docs/plans/archive/adopt-first-track-only.md)

- [x] 2026-08-07 曲にリアルワードの指示文ではなく具体的なワードを表示する — 楽曲詳細の「LLM への入力全文」末尾に出力条件のプロンプト指示文(`- realWorldWords: …5〜8 個`)が表示され、具体的なワードが出ていないように見えた。指示は構造化出力スキーマの description と重複していたためプロンプトから削除(LLM への指示は維持、既存曲の記録はそのまま)。あわせて楽曲一覧の各曲(評価ボタンの下)に realWorldWords をピル型タグで常時表示(ワードの無い旧データはタグ行ごと非表示。詳細内の「リアルワード」セクションは記録として存置)。単体でプロンプトに指示行が無いこと、ヘッドレス Chrome でタグ表示を確認。プラン: [docs/plans/archive/track-real-word-display.md](docs/plans/archive/track-real-word-display.md)

- [x] 2026-08-07 天気の位置を座標ではなく都市名で選択できるようにする — 設定ページの緯度・経度入力を 47 都道府県庁所在地のドロップダウンに置き換え(選択で都市名+座標を 1 回の PUT で同時保存し不整合を防ぐ)。settings に `weather_city`(既定「東京」)を追加し、プロンプトの天気見出しを「### 今日の天気(<都市名>)」に変更。座標は Open-Meteo Geocoding のローマ字クエリ+JP フィルタで一括取得して `settings.js` に定数として埋め込み(同 API は日本語表記のクエリにヒットせず、国土地理院 AddressSearch は候補品質が悪いため検索型は見送り。鳥取が北海道の同名地点を拾う誤りは個別取得で修正)。隔離サーバーで PUT/GET・不正値 400・札幌の天気取得を確認、設定ページはヘッドレス Chrome で表示確認。プラン: [docs/plans/archive/weather-city-selection.md](docs/plans/archive/weather-city-selection.md)

- [x] 2026-08-07 管理画面を一般的なレイアウト(左ペインメニュー)に変更 — 3 ページ(楽曲一覧・パラメータ一覧・設定)を、左サイドバー(アプリ名+縦並びメニュー、現在ページをアクセント背景でハイライト)+右コンテンツの 2 ペイン構成に変更。静的 3 ページなのでテンプレートは導入せずサイドバーは各 HTML に複製。残クレジット+チャージリンクは楽曲一覧のページヘッダーへ移動(`app.js` の id 不変で JS 3 本は無変更)。700px 以下はサイドバーを上部横並びナビに切替(折り返し許可で実機幅にも対応)。ヘッドレス Chrome でデスクトップ 1100px の 3 ページ+モバイル 500px を確認。プラン: [docs/plans/archive/admin-sidebar-layout.md](docs/plans/archive/admin-sidebar-layout.md)

- [x] 2026-08-07 管理画面で評価ボタンを押したかどうか分からないので直す — 👍/👎 は未選択でも絵文字がフルカラーで背景差しか無く状態が判別しづらかったため、未選択を `filter: grayscale(1)` + `opacity: 0.55` で減光し、選択済み(フルカラー絵文字+アクセント背景)との差を強調(`style.css` の `.rate-btn` のみ。JS のトグルロジックは変更なし、iOS は対象外)。ヘッドレス Chrome で選択済み/未選択の見た目の差を確認。プラン: [docs/plans/archive/rating-button-visibility.md](docs/plans/archive/rating-button-visibility.md)

- [x] 2026-08-07 管理画面の生成機能を毎日の自動生成と同じにする — 生成フォーム(プリセット選択チップ・自由テキスト・インストチェック)をボタンのみに置き換え、既存の `POST /admin/api/daily/run`(プロファイル更新 → 冒険日判定 → LLM 生成 → Suno 送信。`last_daily_date` は更新しないため当日のスケジュール実行は別途走る)を呼ぶ方式に変更。プリセット管理はパラメータ一覧ページ `/admin/presets.html`(新規 `presets.js`。一覧・追加・編集・削除)へ移動し、ヘッダーに相互リンクを追加。`POST /api/generate`(manual フロー)は iOS の生成画面が使うため存置。不要になったチップ・textarea・checkbox の CSS を削除。typecheck + ヘッドレス Chrome で両ページの表示確認済み(生成ボタンの実行はクレジット消費のため未実施)。プラン: [docs/plans/archive/admin-generate-daily-flow.md](docs/plans/archive/admin-generate-daily-flow.md)

- [x] 2026-08-07 管理画面の音楽生成の上部のパラメータの選択が見ずらいので修正 — プリセット選択チップをラベル列(4.5em 固定)+チップ列の 2 カラム構造に変更(`app.js` でチップ群を `.chips` コンテナに包み、折り返してもチップ列内で整列)。カテゴリ間の余白を 6→12px に拡大しグループ境界を明確化、ラベル 0.75→0.8rem・チップ上下パディングも微増。ヘッドレス Chrome のビフォー/アフター比較で確認。※検証中に見えたスマホ幅の右はみ出しはヘッドレス Chrome の最小ウィンドウ幅 500px による artifact で、実際のはみ出しは無し(実測 scrollWidth == viewport)。プラン: [docs/plans/archive/generate-presets-legibility.md](docs/plans/archive/generate-presets-legibility.md)

- [x] 2026-08-07 音楽生成のプロンプトの日本語訳を管理画面に表示 — `SongPlan` に `styleJa`(スタイルプロンプトの日本語訳)を追加し LLM がスタイルと同時に生成(追加コールなし)。`tasks.style_ja` を後方互換マイグレーションで追加して保存、tracks/tasks API に `styleJa` を追加し、管理画面の楽曲詳細で「スタイル」の下に「スタイル(日本語訳)」を表示(旧データは NULL → 非表示)。隔離 DB で保存・取得を検証、実 DB マイグレーション・API 出力確認済み(訳は次回生成分から入る)。プラン: [docs/plans/archive/style-ja-display.md](docs/plans/archive/style-ja-display.md)

- [x] 2026-08-07 生成に使用しているパラメータを全て管理画面に表示する — `tasks` に `llm_model` / `llm_prompt`(LLM に送った入力全文 = プロファイル・プリセット・直近スタイル・モード指示)を後方互換マイグレーションで追加し、`generateSongPlan` の戻り値 `{ plan, llmModel, llmPrompt }` 経由で保存。tracks/tasks API に `prompt` / `instrumental` / `sunoModel` / `llmModel` / `llmPrompt` を追加し、管理画面の楽曲詳細(「歌詞・生成パラメータ」)にモード・リクエスト・インスト・Suno モデル・LLM モデル・LLM 入力全文を表示(旧データの欠落項目は非表示)。生成中タスクに毎日/冒険日バッジ。隔離 DB で保存・取得を検証、実 DB マイグレーション・API 出力確認済み(`llm_model` 等は次回生成分から入る)。プラン: [docs/plans/archive/admin-show-generation-params.md](docs/plans/archive/admin-show-generation-params.md)

- [x] 2026-08-07 自動生成の仕組みを確認 — `src/scheduler.ts` を確認。1 分間隔で設定タイムゾーンの現在時刻をチェックし「実行時刻以降かつ当日未生成」なら プロファイル更新 → 冒険日判定 → LLM 生成 → Suno 送信 を実行(初回起動は当日生成済み扱い、失敗時 30 分後再試行、停止中跨ぎは起動時に追い生成)。仕様・CLAUDE.md との矛盾なし

- [x] 2026-08-07 アプリに評価ボタンがない — iOS の楽曲一覧の各行に管理画面と同じトグル式 👍/👎 ボタンを追加(`POST /api/tracks/:id/rating`。行タップ再生は維持し、評価ボタンは `.borderless` の独立タップ領域。`Track` に `rating` 追加、解除時は `"rating": null` をカスタム encode で送信)。UI テスト `RatingUITests` 追加(トグル後に元の評価へ復元するのでデータを汚さない)、既存再生テストと合わせシミュレータで 2 件パス。プラン: [docs/plans/archive/ios-rating-buttons.md](docs/plans/archive/ios-rating-buttons.md)

- [x] 2026-08-07 評価を良い悪いの２択にする — ★(お気に入り)を廃止し 👍/👎 の 2 択に統一。起動時マイグレーションで `tracks.favorite` を DROP(★のみで未評価の曲は 👍 に変換。実 DB で確認)。`POST /api/tracks/:id/rating` は `{ rating: 1 | -1 | null }` のみ受付(favorite 送信は 400)、tracks API レスポンス・プロファイル更新 LLM 入力・管理画面の ★ ボタンも削除。プラン: [docs/plans/archive/rating-two-choice.md](docs/plans/archive/rating-two-choice.md)

- [x] 2026-08-07 管理画面からポイントチャージする画面へリンクをつける — ヘッダーの残クレジット表示の隣に kie.ai のチャージ画面(`https://kie.ai/billing`)を別タブで開く「チャージ」リンクを常時表示で追加。プラン: [docs/plans/archive/admin-credits-charge-link.md](docs/plans/archive/admin-credits-charge-link.md)

- [x] 2026-08-07 音楽生成エンジンの実装 — 全 4 Phase 完了。①評価基盤(`tracks` に rating/favorite、`POST /api/tracks/:id/rating`、管理画面の 👍/👎/★ トグル)②プリセット管理(`presets` テーブル・CRUD API・初期 37 件 seed・管理画面 UI)③LLM 生成パイプライン(`src/llm.ts`: Claude API `claude-sonnet-5` の構造化出力でスタイル・英語歌詞・日本語訳・タイトル・狙いを生成し Suno へ `customMode: true` 送信。`profile` テーブル、`GET /api/profile`、管理画面の生成フォームをプリセットチップ選択化+楽曲に歌詞・狙い表示)④毎日の自動生成(`src/scheduler.ts`: settings テーブル+`GET/PUT /api/settings`、1 分間隔で PT 6:00 判定・停止中跨ぎは起動時追い生成・初回起動は当日を生成済み扱い、冒険日判定 20%、`POST /api/daily/run`)。実生成 E2E 2 回で検証(customMode で歌詞どおりの曲・評価→プロファイル更新→好みを反映した自動生成を確認)。時刻判定は DST 切替含む 10 ケースをテスト。仕様: [docs/specs/music-generation.md](docs/specs/music-generation.md)、プラン: [docs/plans/archive/music-generation-engine.md](docs/plans/archive/music-generation-engine.md)

- [x] 2026-08-06 `/audio/*` `/images/*` を認証付き配信にする — 無認証の `/audio/*` `/images/*` を廃止し、`/api/audio|images/*`(X-API-Secret 必須。iOS は AVURLAsset のヘッダ注入 + カバー画像の自前ローダー)と `/admin/audio|images/*`(エッジの Cloudflare Access)の二重マウントへ移行(2545ab6)。無認証経路はアプリ層では `/health` のみ。本番デプロイ・外形確認済み(2026-08-06: 旧経路 404 / `/api/audio/*` secret 無し 401・有りはルート到達〔本番は tracks 0 件のため実ファイルでの 200/206 確認は初回生成後 — g3plus-ops TODO〕/ `/admin/audio/*` は Access ログインへ 302)。残作業は g3plus-ops TODO で追跡: **Cloudflare の Cache Rule 作成(ホスト全体 Bypass。エッジキャッシュは認証評価前に配信されるため必須)** と iOS 実機の再ビルド・インストール(`run-ios-device.sh`。`audioUrl` 形式変更の破壊的変更のため旧アプリは再生不可)。プラン: [docs/plans/archive/media-auth.md](docs/plans/archive/media-auth.md)

- [x] 2026-08-06 Web 管理画面を `/` 配信から `/admin` 配下へ移動する(Cloudflare Access をパス限定で掛けるため)— サーバ側は `/admin/*` マウント + `/` `/admin` → `/admin/` リダイレクト(7ff057e)で対応しデプロイ済み。Cloudflare Access は `music.chobi.me` の Path `admin` 1 アプリ(Google IdP のみ)に整理され、暫定のパス分割(`/api` `/audio` `/images` Bypass + root Allow)は解消。本番外形確認(2026-08-06): `/admin/*`(`/admin/api/*` 含む)が Access ログインへ 302(ログインページは Google のみ・OTP 無し)、`/` は Access 素通りでアプリが `/admin/` へ 302、secret 付き `/api/ping` 200・無しは 401、`/audio/*` `/images/*` は従来どおり配信。プラン: [docs/plans/archive/admin-path.md](docs/plans/archive/admin-path.md)

- [x] 2026-08-06 Web 管理画面を API Secret なしで使えるようにする — API ルートをサブルーター化して `/api`(X-API-Secret 必須・iOS 向け)と `/admin/api`(アプリ層無認証・管理画面向け)の両方にマウントし、管理画面 JS から secret 付与・localStorage 保存・401 時の入力プロンプトを削除。本番はエッジの Cloudflare Access が `/admin` ごと保護する(ローカル LAN では `/admin/api/*` が無認証になる点は許容)。プラン: [docs/plans/archive/admin-no-secret.md](docs/plans/archive/admin-no-secret.md)

- [x] 2026-08-06 サーバの接続先を https://music.chobi.me — `run-ios-device.sh` の既定接続先を本番 `https://music.chobi.me` に変更(`--local` で従来の Mac LAN IP 接続、ビルド前に `/api/ping` 疎通確認を追加)。実機ビルド・インストール・起動を確認。**注意**: ローカル `.env` の `API_SECRET` は本番と不一致(401)のため、`.env` を本番の値に更新するかアプリの設定画面で本番 Secret の入力が必要。プラン: [docs/plans/archive/backend-endpoint-music-chobi-me.md](docs/plans/archive/backend-endpoint-music-chobi-me.md)

- [x] 2026-08-06 iOS アプリ(楽曲の一覧・再生、生成リクエストの送信)と API 認証方式 — `/api/*` を `X-API-Secret` ヘッダ認証に(esl-learning-assistant と同方式。timing-safe 比較・起動時 fail-fast・Web 管理画面は localStorage で対応)。`ios/` に XcodeGen + SwiftUI アプリ(楽曲一覧・AVPlayer 再生・生成リクエスト・設定画面)を作成し、UI テストで再生フローを検証、`run-ios-device.sh` で実機インストール・起動まで確認。プラン: [docs/plans/archive/ios-app-and-api-auth.md](docs/plans/archive/ios-app-and-api-auth.md)

- [x] 2026-08-06 音楽生成の仕組みを決める — 会話で設計を確定。LLM(Sonnet 5)が好みプロファイル+プリセットからスタイル・英語歌詞・日本語訳を生成し customMode で Suno へ。評価は 👍/👎+★、冒険日 20%、毎朝 6:00(PT)に 1 リクエスト(2 曲)。仕様: [docs/specs/music-generation.md](docs/specs/music-generation.md)

- [x] 2026-08-06 Web管理画面の作成と音楽の生成と再生 — `server/`(Hono + node:sqlite)に生成 API・ポーリングジョブ・管理画面を実装。E2E で実生成 1 回(2 曲保存・再生)を確認。起動: `cd server && npm start` → http://localhost:3014。プラン: [docs/plans/archive/web-admin-music-generation.md](docs/plans/archive/web-admin-music-generation.md)

- [x] 2026-08-06 Suno との連携方式(公式 API の可否、認証方法、MCP サーバ)を調査する — 結果: [docs/specs/suno-api.md](docs/specs/suno-api.md)
- [x] 2026-08-06 サードパーティ Suno API のアカウントを作成して API キーを発行し、少額クレジットで生成フローを検証する — sunoapi.org は Google ログイン不可のため同一運営の kie.ai で検証。スクリプト: `scripts/verify-sunoapi.mjs`、実測値: [docs/specs/suno-api.md](docs/specs/suno-api.md)、プラン: [docs/plans/archive/sunoapi-org-verification.md](docs/plans/archive/sunoapi-org-verification.md)
