# TODO

- [ ] 毎日作成をアーティストと曲から選択して生成するのに変更
- [ ] プロンプト生成で web_search を使って外部情報を積極的に参照する — 現状は `llm.ts` が `tools` 無しの Claude API 呼び出しで、参照曲の情報は登録時に iTunes から取った 5 項目（アーティスト・曲名・アルバム・リリース年・ジャンル）だけ。曲を知らなければ「アーティストの作風からの推定」で書かせている。web_search で実際の曲情報（BPM・キー・編成・レビュー等）を調べてから style を書かせる方式に切り替える。決めること: ①対象モード（artist だけか、daily / manual にも広げるか）②検索結果が別の同名曲を指すリスクへの対処 ③固有名詞の混入リスクが上がる点（既存の `properNounsIn()` チェックと再生成でどこまで防げるか）④レイテンシ・コストの増加（現状 1 曲あたり LLM 呼び出し 44 秒）⑤`intent` の「根拠」表記の扱い。着手時は `docs/specs/music-generation.md` の非目標「Web 検索・音源解析による参照曲の特徴補強」の記述も更新する