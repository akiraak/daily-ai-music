# TODO

- [ ] リアルワード: 外部リソース(ニュース・天気)から曲の着想ワードを作り、保存・使用制限で似た曲の連続生成を防ぐ(同一ワードは直近 30 日で 2 回まで、古いワードは自動で再利用可能に)[plan](docs/plans/real-world-words.md)
  - [x] Phase 1: 注入基盤 + ニュース(Google News RSS)+ パラメータ設定画面新設(ニュース ON/OFF・毎日の自動生成設定)
  - [x] Phase 2: リアルワードの生成・保存(real_world_words テーブル)+楽曲詳細に表示
  - [x] Phase 3: 使用回数の集計とプロンプトでの制限+検証リトライ
  - [x] Phase 4: 天気(Open-Meteo)+ 設定画面に天気 ON/OFF・位置設定を追加
  - [x] Phase 5: パラメータ一覧ページに使用ワードと残り回数を表示(任意)
  - [ ] 稼働後チェック: Sonnet 5 のパラメータ(realWorldWords 等)生成精度を確認し、悪ければ `.env` の `LLM_MODEL` を claude-opus-5 に変更

- [ ] 使用プリセットの保存と評価による優先度付け+プロファイル文書の廃止: LLM の構造化出力に採用プリセットを追加してタスク単位で保存し(現状は llm_prompt・style から推定するしかない)、👍 された曲のプリセットは次回生成で使用の優先度を上げる(👎 は下げる。プリセット別の 👍/👎 集計を要素プールに併記して注入)。**好みの管理はプリセット側の評価集計に一本化し、プロファイル文書は一度廃止する**(生成プロンプト注入・毎朝の更新 LLM コール・API・管理画面パネルを削除。DB のテーブルと過去データは残し、プリセットで表せない細部の好みが必要になったら役割を絞って復活を検討)[plan](docs/plans/preset-priority.md)
  - [x] Phase 1: 使用プリセットの記録(LLM 出力に usedPresets 追加 + task_presets テーブル + API 露出 + 管理画面の楽曲詳細に表示)
  - [ ] Phase 2: 評価集計の優先度付け(集計クエリ + 要素プール行に 👍/👎 併記 + モード別指示 + presets 管理ページに集計列)
  - [ ] Phase 3: プロファイル文書の廃止(生成プロンプト・runDaily・updateProfile・GET /api/profile・管理画面パネル・iOS profileUpdated の削除。DB テーブルは残す)
  - [ ] Phase 4: 生成プロンプトの分かりやすい表示(管理画面の LLM 入力全文をセクション分解表示に置き換え+iOS の楽曲詳細に llmPrompt の折りたたみ整形表示を追加)
  - [ ] Phase 5(任意): iOS 楽曲詳細に使用プリセット表示

- [ ] 生成曲の音源解析によるプリセット自動追加(Gemini): 完成した曲の mp3 を Gemini API に渡して実際に鳴っている特徴ワード(ジャンル・楽器・ムード・テンポ・歌声)を抽出し、既存プリセットの語彙と照合して有意に異なる(どの value とも意味が重ならない)ワードがあればプリセットに追加する。LLM が Suno に指示した内容(usedPresets)ではなく音源の実測なので、Suno がスタイル指示から外れた場合の実態も拾える。Claude API は音声入力非対応のため解析は Gemini(音声は 32 トークン/秒 ≒ 1 曲数千トークンでコストほぼゼロ。`.env` に `GEMINI_API_KEY` 追加)。自動追加分は由来(source)を記録して管理画面で区別表示し、評価集計(👍/👎)で淘汰できるようにする。「使用プリセットの保存と評価による優先度付け」タスクの完了後に着手し、照合基準・剪定ルール・追加上限などの方式詳細は着手時にプランで検討
