# TODO

- [ ] アーティスト経由生成の本番反映 — サーバーを本番(music.chobi.me)へデプロイし、実機アプリを再インストール(`./run-ios-device.sh`)。API は追加のみで既存キーの形状を変えていないため旧アプリ×新サーバーは完全互換 = **サーバーデプロイのみが必須**で、実機の再インストールはアプリからアーティスト経由生成を使いたくなったときでよい(新アプリ×旧サーバーはアーティスト画面が 404 エラー表示になるため、その際もサーバー先行なら問題なし)。反映後は本番でアーティストを 1 人登録して 1 曲生成し、実聴で確認する
