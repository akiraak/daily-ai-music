# TODO

- [ ] 歌声の言語(既定 日本語)の本番反映 — サーバーをデプロイすると `vocal_language` 未設定 = `ja` なので、**次の毎日の自動生成から日本語で歌う曲に変わる**(`SUNO_MODEL` の既定も `V5_5` に上がる)。**旧アプリとの互換は保たれている**(応答は `lyricsLang` のキー追加のみで旧アプリは無視する)ため、サーバー先行デプロイでよい。`tasks.lyrics_lang` の追加は起動時の `ALTER TABLE ADD COLUMN` で自動・冪等なので DB の退避は不要。実機アプリの更新は設定画面から言語を切り替えたくなったときでよい
